{
    "name": "dailypick",
    "version": "0.0.1",
    "source": "index.html",
    "description": "A fun and interactive collection of mini-games designed to liven up team activities! Perfect for adding a bit of engagement to daily meetings or team-building sessions",
    "browserslist": "> 0.5%, last 2 versions, not dead",
    "scripts": {
        "start": "parcel $(find . -name index.html  -not -path './dist/*') --no-source-maps && cp -r public/* dist/",
        "build": "parcel build $(find . -name index.html) --no-source-maps && cp -r public/* dist/"
    },
    "devDependencies": {
        "@parcel/packager-raw-url": "^2.14.4",
        "@parcel/transformer-jsonld": "^2.14.4",
        "@parcel/transformer-webmanifest": "^2.14.4",
        "parcel": "^2.14.0",
        "svgo": "^3.3.2"
    }
}