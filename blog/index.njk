---
layout: base.njk
title: Daily Pick Blog
description: Articles, insights, and updates from the Daily Pick team. Discover tips on team engagement, productivity, and making work more fun.
pagination:
  data: collections.post
  size: 5
  alias: posts
  reverse: true
permalink: /blog/{% if pagination.pageNumber > 0 %}page/{{ pagination.pageNumber + 1 }}/{% endif %}index.html
eleventyNavigation:
  key: Blog
  order: 2
---

<div class="main-container blog-listing">
    <h1>{{ title }}</h1>
    <p class="sub-header">{{ description }}</p>

    {% if posts and posts.length > 0 %}
        <ul class="post-list">
            {%- for post in posts -%}
            <li class="post-list-item">
                <h2><a href="{{ post.url | url }}">{{ post.data.title }}</a></h2>
                <p class="post-meta">
                    <time datetime="{{ post.date.toISOString() }}">{{ post.date.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) }}</time>
                    {% if post.data.tags and post.data.tags.length > 0 %}
                        <span class="post-tags"> | Tags: {% for tag in post.data.tags %}<span class="tag">{{ tag }}</span>{% if not loop.last %}, {% endif %}{% endfor %}</span>
                    {% endif %}
                </p>
                {% if post.data.description %}<p class="post-excerpt">{{ post.data.description }}</p>{% endif %}
            </li>
            {%- endfor -%}
        </ul>

        <nav class="pagination">
            <ul class="pagination-list">
                {% if pagination.href.previous %}
                    <li><a href="{{ pagination.href.previous | url }}">‹ Newer</a></li>
                {% endif %}
                {%- for pageEntry in pagination.pages %}
                    <li><a href="{{ pagination.hrefs[ loop.index0 ] | url }}" {% if page.url == pagination.hrefs[ loop.index0 ] %}aria-current="page"{% endif %}>{{ loop.index }}</a></li>
                {%- endfor %}
                {% if pagination.href.next %}
                    <li><a href="{{ pagination.href.next | url }}">Older ›</a></li>
                {% endif %}
            </ul>
        </nav>
    {% else %}
        <p>No blog posts yet. Check back soon!</p>
    {% endif %}
</div>
